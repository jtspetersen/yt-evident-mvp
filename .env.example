# Evident Video Fact Checker - Environment Configuration Template
# Copy this file to .env and customize for your setup

# ============================================================================
# SERVICE URLS
# ============================================================================
# Ollama LLM service (use 'ollama' hostname in Docker network)
EVIDENT_OLLAMA_BASE_URL=http://ollama:11434

# SearXNG metasearch engine (use 'searxng' hostname in Docker network)
EVIDENT_SEARXNG_BASE_URL=http://searxng:8080

# ============================================================================
# MODELS (Selected by setup wizard based on your hardware)
# ============================================================================
# Extraction model (lightweight, fast claim extraction)
EVIDENT_MODEL_EXTRACT=qwen3:8b

# Verification model (strong reasoning for claim verification)
EVIDENT_MODEL_VERIFY=qwen3:30b

# Writing model (creative script generation)
EVIDENT_MODEL_WRITE=gemma3:27b

# ============================================================================
# TEMPERATURES (LLM creativity/randomness)
# ============================================================================
# Extract: Low randomness for consistent JSON output
EVIDENT_TEMPERATURE_EXTRACT=0.1

# Verify: Zero randomness for deterministic fact-checking
EVIDENT_TEMPERATURE_VERIFY=0.0

# Write: Moderate randomness for creative script writing
EVIDENT_TEMPERATURE_WRITE=0.5

# ============================================================================
# HARDWARE (Detected by setup wizard)
# ============================================================================
# GPU enabled (true/false)
EVIDENT_GPU_ENABLED=false

# GPU type (nvidia/amd/none)
EVIDENT_GPU_TYPE=none

# GPU VRAM in GB (detected via nvidia-smi or rocm-smi)
EVIDENT_GPU_MEMORY_GB=0

# System RAM in GB (detected via psutil)
EVIDENT_RAM_GB=16

# CPU cores (detected via psutil)
EVIDENT_CPU_CORES=4

# ============================================================================
# BUDGETS & LIMITS
# ============================================================================
# Maximum claims to extract per video
EVIDENT_MAX_CLAIMS=25

# Maximum sources to fetch per claim
EVIDENT_MAX_SOURCES_PER_CLAIM=5

# Total URL fetches allowed per run
EVIDENT_MAX_FETCHES_PER_RUN=80

# Timeout for each URL fetch (seconds)
EVIDENT_FETCH_TIMEOUT_SEC=25

# Snippets per source
EVIDENT_SNIPPETS_PER_SOURCE=4

# Maximum characters per snippet
EVIDENT_SNIPPET_MAX_CHARS=1200

# ============================================================================
# CACHE & LOGGING
# ============================================================================
# URL cache TTL (days)
EVIDENT_CACHE_TTL_DAYS=7

# Log level (DEBUG|INFO|WARNING|ERROR)
EVIDENT_LOG_LEVEL=INFO

# Timezone for output timestamps
EVIDENT_TIMEZONE=America/Los_Angeles

# ============================================================================
# SECOND PASS SETTINGS (for high-severity INSUFFICIENT EVIDENCE claims)
# ============================================================================
# Enable second pass evidence retrieval
EVIDENT_SECOND_PASS_ENABLED=true

# Maximum claims to retry with second pass
EVIDENT_SECOND_PASS_MAX_CLAIMS=12

# Extra sources per claim in second pass
EVIDENT_SECOND_PASS_EXTRA_SOURCES=5

# Extra fetch budget for second pass
EVIDENT_SECOND_PASS_EXTRA_FETCHES=60
